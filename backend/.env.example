PORT=5000
NODE_ENV=production
MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/realtime-chat

# ── RENDER (production) — REQUIRED ──
# Your Vercel frontend URL (no trailing slash). Comma-separate preview URLs if needed.
FRONTEND_URL=https://YOUR-APP.vercel.app

CLERK_SECRET_KEY=sk_live_...
